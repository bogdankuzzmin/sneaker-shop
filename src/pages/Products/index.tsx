import React from 'react';

import Layout from '@components/Layout';
import ProductList from '@components/ProductList';

const Products = () => (
  <Layout title="Esplora">
    <ProductList />
  </Layout>
);

export default Products;